apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    uid: prometheus-tryl          # 대시보드 JSON이 이 uid로 데이터소스를 참조한다
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: true
