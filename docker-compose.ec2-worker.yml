# Worker 전용 EC2에서 실행: RDS/Redis/S3는 외부. 이미지는 미리 pull (예: ECR)
# 사용: .env.production 준비 후 → docker compose -f docker-compose.ec2-worker.yml up -d
version: "3.9"

services:
  worker:
    image: tryl-worker:latest
    env_file:
      - .env.production
    restart: unless-stopped
