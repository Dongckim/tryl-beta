# API 전용 EC2에서 실행: RDS/Redis는 외부. 이미지는 미리 pull (예: ECR)
# 사용: .env.production 준비 후 → docker compose -f docker-compose.ec2-api.yml up -d
version: "3.9"

services:
  api:
    image: tryl-api:latest
    env_file:
      - .env.production
    ports:
      - "8000:8000"
    restart: unless-stopped
